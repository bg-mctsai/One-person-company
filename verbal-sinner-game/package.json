{
  "name": "verbal-sinner-game",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm run build",
    "deploy:production": "npm run build && firebase deploy --only hosting",
    "deploy:production:with-firestore": "npm run build && firebase deploy --only hosting,firestore",
    "deploy:production:all": "npm run build && firebase deploy",
    "deploy:production:hosting": "npm run build && firebase deploy --only hosting",
    "deploy:production:functions": "firebase deploy --only functions",
    "deploy:production:firestore": "firebase deploy --only firestore",
    "deploy:production:storage": "firebase deploy --only storage",
    "deploy:production:rules": "firebase deploy --only firestore:rules,storage:rules"
  }
}

