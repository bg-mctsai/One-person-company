# Firebase 設定測試指南

## 🚀 正確的測試方式

### 步驟 1：啟動開發伺服器

在 **PowerShell 終端機**中執行：

```powershell
cd D:\One-person-company\verbal-sinner-game\frontend
npm start
```

等待編譯完成，瀏覽器會自動開啟（通常是 http://localhost:3000）

### 步驟 2：打開瀏覽器 Console

1. 在瀏覽器中按 **F12** 或 **右鍵 → 檢查**
2. 切換到 **Console** 標籤
3. 你應該會看到：
   ```
   ✅ Firebase 配置已載入
      Project ID: verbal-sinnergit-7341965-90a2b
      Auth Domain: verbal-sinnergit-7341965-90a2b.firebaseapp.com
      💡 在 Console 中輸入 testFirebase() 來測試連線
   ```

### 步驟 3：在瀏覽器 Console 中執行測試

在 **瀏覽器的 Console**（不是 PowerShell）中輸入：

```javascript
testFirebase()
```

然後按 **Enter**

---

## ✅ 預期結果

如果設定正確，你會看到：

```
🧪 開始測試 Firebase 設定...

📋 測試 1：檢查環境變數
┌─────────────┬──────────┐
│ (index)     │ Values   │
├─────────────┼──────────┤
│ apiKey      │ ✅ 已設定 │
│ authDomain  │ ✅ 已設定 │
│ projectId   │ ✅ 已設定 │
│ ...         │ ...      │
└─────────────┴──────────┘

🔐 測試 2：匿名登入
✅ 匿名登入成功！
   UID: xxxxxx...

📝 測試 3：初始化用戶進度
✅ 用戶進度初始化成功！
   Firestore 路徑: users/xxxxxx/profile/progress

📖 測試 4：讀取用戶進度
✅ 讀取用戶進度成功！
   進度資料: { currentMoment: null, completedMoments: [], ... }

🔥 測試 5：直接讀取 Firestore
✅ Firestore 讀取成功！
   文件資料: { ... }

👤 測試 6：檢查當前用戶
✅ 當前用戶存在
   UID: xxxxxx...
   匿名: true

🎉 測試完成！

💡 如果所有測試都通過，表示 Firebase 設定正確！
   可以開始使用 Roo Code 進行開發了。
```

---

## ❌ 常見錯誤

### 錯誤 1：`testFirebase is not defined`

**原因**：在 PowerShell 中執行，而不是瀏覽器 Console

**解決**：
1. 確認開發伺服器已啟動（`npm start`）
2. 在瀏覽器中打開 http://localhost:3000
3. 按 F12 打開開發者工具
4. 在 **瀏覽器的 Console** 中執行 `testFirebase()`

### 錯誤 2：`❌ 匿名登入失敗`

**可能原因**：
1. Firebase 專案未啟用 Anonymous Auth
2. 環境變數未正確設定
3. 網路連線問題

**解決**：
1. 前往 Firebase Console：https://console.firebase.google.com/
2. 選擇專案：`verbal-sinnergit-7341965-90a2b`
3. 左側選單 → **Authentication** → **Sign-in method**
4. 找到 **Anonymous** → 點擊 → **啟用** → **儲存**
5. 重新執行測試

### 錯誤 3：`❌ Firebase 環境變數未設定！`

**原因**：`.env.local` 檔案未正確設定

**解決**：
1. 確認 `frontend/.env.local` 檔案存在
2. 確認所有變數都已填入（參考 `SETUP_FIREBASE.md`）
3. **重新啟動開發伺服器**（重要！）
   ```powershell
   # 停止目前的伺服器（Ctrl+C）
   # 然後重新啟動
   npm start
   ```

---

## 🔍 快速檢查清單

- [ ] 開發伺服器已啟動（`npm start`）
- [ ] 瀏覽器已打開 http://localhost:3000
- [ ] 瀏覽器 Console 已打開（F12）
- [ ] 在瀏覽器 Console 中執行 `testFirebase()`
- [ ] Firebase Console 中已啟用 Anonymous Auth

---

## 💡 提示

- **不要**在 PowerShell 終端機中執行 `testFirebase()`
- **要**在瀏覽器的 Console 中執行
- 如果測試失敗，檢查 Console 中的錯誤訊息
- 所有測試都通過後，可以開始使用 Roo Code 進行開發
